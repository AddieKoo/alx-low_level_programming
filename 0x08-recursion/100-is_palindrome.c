#include "main.h"
/**
 * is_palindrome - returns 1 if a string is palindrome, else 0
 * @s: string to be checked
 *
 * Return: 1 if s is a palindrome, else 0
 */
int is_palindrome(char *s)
{
int flag = 1;
if (s, 0; _strlen_recursion(s) - 1; &flag);
return (flag);
}
